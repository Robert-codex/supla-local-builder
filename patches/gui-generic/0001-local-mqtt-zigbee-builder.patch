diff --git a/builder.json b/builder.json
index 36b76a3b..66717a30 100644
--- a/builder.json
+++ b/builder.json
@@ -1134,6 +1134,48 @@
 				"desc": "Paramètres de l´appareil->Autre. Il permet le fonctionnement du compteur d´énergie CSE7766."
 			}
 		},
+		"SUPLA_CSE7761": {
+			"name": "Licznik energii CSE7761",
+			"desc": "Ustawienia urządzenia->Inne. Umożliwia obsługę licznika energii CSE7761 przez UART. Aktualna implementacja wystawia jeden kanał pomiarowy naraz (IA albo IB).",
+			"defOn": false,
+			"en": {
+				"name": "Energy meter CSE7761",
+				"desc": "Device settings->Other. Enables the CSE7761 energy meter over UART. The current implementation exposes one measurement channel at a time (IA or IB)."
+			},
+			"es": {
+				"name": "Contador de energía CSE7761",
+				"desc": "Configuración del dispositivo->Otro. Permite el funcionamiento del contador de energía CSE7761 por UART. La implementación actual expone un canal de medición a la vez (IA o IB)."
+			},
+			"de": {
+				"name": "Energiezähler CSE7761",
+				"desc": "Geräteeinstellungen->Andere. Aktiviert den CSE7761-Energiezähler über UART. Die aktuelle Implementierung stellt jeweils nur einen Messkanal bereit (IA oder IB)."
+			},
+			"fr": {
+				"name": "Compteur d´énergie CSE7761",
+				"desc": "Paramètres de l´appareil->Autre. Active le compteur d´énergie CSE7761 via UART. L´implémentation actuelle expose un seul canal de mesure à la fois (IA ou IB)."
+			}
+		},
+		"SUPLA_BL0930": {
+			"name": "Licznik energii BL0930",
+			"desc": "Ustawienia urządzenia->Inne. Umożliwia obsługę BL0930 przez wejście impulsowe CF. Aktualny driver wyznacza moc czynną i energię z impulsów CF; napięcie i prąd nie są wystawiane.",
+			"defOn": false,
+			"en": {
+				"name": "Energy meter BL0930",
+				"desc": "Device settings->Other. Enables BL0930 support through the CF pulse output. The current driver exposes active power and energy from CF pulses; voltage and current are not exposed."
+			},
+			"es": {
+				"name": "Contador de energía BL0930",
+				"desc": "Configuración del dispositivo->Otro. Permite el funcionamiento del BL0930 por la salida de pulsos CF. El controlador actual expone potencia activa y energía; no expone tensión ni corriente."
+			},
+			"de": {
+				"name": "Energiezähler BL0930",
+				"desc": "Geräteeinstellungen->Andere. Aktiviert BL0930 über den CF-Impulsausgang. Der aktuelle Treiber liefert Wirkleistung und Energie; Spannung und Strom werden nicht bereitgestellt."
+			},
+			"fr": {
+				"name": "Compteur d´énergie BL0930",
+				"desc": "Paramètres de l´appareil->Autre. Active BL0930 via la sortie d´impulsions CF. Le pilote actuel expose la puissance active et l´énergie; la tension et le courant ne sont pas exposés."
+			}
+		},
 		"SUPLA_ADE7953": {
 			"name": "Licznik energii ADE7953",
 			"desc": "Ustawienia urządzenia->I2C. Umożliwia obsługę licznika energii ADE7953.",
diff --git a/platformio.ini b/platformio.ini
index 8a9446ff..e6769231 100644
--- a/platformio.ini
+++ b/platformio.ini
@@ -225,6 +225,7 @@ lib_deps =
     Z2S_Library
 build_flags = 
     ${common.build_flags}
+    -D SUPLA_ZIGBEE_GATEWAY
     -D CORE_DEBUG_LEVEL=5
     -D ARDUINO_ESP32C6_DEV
     ;-D ARDUINO_USB_MODE=1
@@ -316,4 +317,4 @@ lib_ignore =
     ${common_no_libs.lib_ignore}
 build_flags =
     ${common.build_flags}
-lib_ldf_mode = chain+
\ No newline at end of file
+lib_ldf_mode = chain+
diff --git a/src/GUI-Generic.ino b/src/GUI-Generic.ino
index e9f52de2..125d34aa 100644
--- a/src/GUI-Generic.ino
+++ b/src/GUI-Generic.ino
@@ -62,6 +62,10 @@ void setup() {
   new Supla::boneIO();
 #endif
 
+#if defined(SUPLA_ZIGBEE_GATEWAY) && defined(CONFIG_IDF_TARGET_ESP32C6)
+  Supla::GUI::ZigbeeGatewayMode::setup();
+#endif
+
 #ifdef GUI_SENSOR_I2C_EXPENDER
   Expander = new Supla::Control::ConfigExpander();
 #endif
@@ -495,6 +499,12 @@ void setup() {
   }
 #endif
 
+#ifdef SUPLA_BL0930
+  if (ConfigESP->getGpio(FUNCTION_CF) != OFF_GPIO) {
+    Supla::GUI::addBL0930(ConfigESP->getGpio(FUNCTION_CF));
+  }
+#endif
+
 #ifdef SUPLA_PZEM_V_3
   // Declaration of the ElectricityMeter pointer
   Supla::Sensor::ElectricityMeter *PZEMv3 = nullptr;
@@ -588,6 +598,15 @@ void setup() {
   }
 #endif
 
+#ifdef SUPLA_CSE7761
+  if (ConfigESP->getGpio(FUNCTION_CSE7761_RX) != OFF_GPIO && ConfigESP->getGpio(FUNCTION_CSE7761_TX) != OFF_GPIO) {
+    Supla::GUI::addCSE7761(
+        ConfigESP->getHardwareSerial(ConfigESP->getGpio(FUNCTION_CSE7761_RX), ConfigESP->getGpio(FUNCTION_CSE7761_TX)),
+        ConfigESP->getGpio(FUNCTION_CSE7761_RX), ConfigESP->getGpio(FUNCTION_CSE7761_TX));
+    improvSerialComponent->disable();
+  }
+#endif
+
 #if defined(SUPLA_MODBUS_SDM) || defined(SUPLA_MODBUS_SDM_ONE_PHASE) || defined(SUPLA_MODBUS_SDM_72_V2)
   if (ConfigESP->getGpio(FUNCTION_SDM_RX) != OFF_GPIO && ConfigESP->getGpio(FUNCTION_SDM_TX) != OFF_GPIO) {
 #if defined(SUPLA_MODBUS_SDM)
@@ -997,6 +1016,10 @@ void loop() {
   const uint32_t now = millis();
   SuplaDevice.iterate();
 
+#if defined(SUPLA_ZIGBEE_GATEWAY) && defined(CONFIG_IDF_TARGET_ESP32C6)
+  Supla::GUI::ZigbeeGatewayMode::iterate();
+#endif
+
 #ifndef SUPLA_CC1101
   uint32_t delay_time = LOOP_INTERVAL;
   if (now - last_loop < LOOP_INTERVAL)
diff --git a/src/GUI-Generic_Config.h b/src/GUI-Generic_Config.h
index 7c9351c6..711e4f68 100644
--- a/src/GUI-Generic_Config.h
+++ b/src/GUI-Generic_Config.h
@@ -70,6 +70,8 @@
 // #define SUPLA_DIRECT_LINKS
 // #define SUPLA_PZEM_V_3
 // #define SUPLA_CSE7766
+// #define SUPLA_CSE7761    // UART driver, exposes channel IA or IB selected in GUI
+// #define SUPLA_BL0930     // CF pulse driver, exposes active power and energy
 // #define SUPLA_DEEP_SLEEP
 // #define SUPLA_DIRECT_LINKS_SENSOR_THERMOMETR
 // #define SUPLA_RF_BRIDGE
diff --git a/src/SuplaCommonPROGMEM.h b/src/SuplaCommonPROGMEM.h
index 1e607c96..b9ab94b9 100644
--- a/src/SuplaCommonPROGMEM.h
+++ b/src/SuplaCommonPROGMEM.h
@@ -258,6 +258,10 @@ const char LOW_STATE_CONTROL[] PROGMEM = S_LOW;
 const char HIGH_STATE_CONTROL[] PROGMEM = S_HIGH;
 const char* const LEVEL_P[] PROGMEM = {LOW_STATE_CONTROL, HIGH_STATE_CONTROL};
 
+const char CURRENT_CHANNEL_A[] PROGMEM = "IA";
+const char CURRENT_CHANNEL_B[] PROGMEM = "IB";
+const char* const CURRENT_CHANNEL_P[] PROGMEM = {CURRENT_CHANNEL_A, CURRENT_CHANNEL_B};
+
 const char POSITION_MEMORY[] PROGMEM = S_POSITION_MEMORY;
 const char* const MEMORY_P[] PROGMEM = {OFF, ON, POSITION_MEMORY};
 
diff --git a/src/SuplaConfigManager.cpp b/src/SuplaConfigManager.cpp
index 3c96f7d9..b9773a3e 100644
--- a/src/SuplaConfigManager.cpp
+++ b/src/SuplaConfigManager.cpp
@@ -31,6 +31,20 @@ const char ConfigFileName[] = "/supla-dev.cfg";
 const char CustomCAFileName[] = "/custom_ca.pem";
 };  // namespace Supla
 
+namespace {
+void enableMqttIfMissing(Supla::Config *config) {
+#ifdef ARDUINO_ARCH_ESP32
+  int8_t mqttEnabled = 0;
+  if (config != nullptr && !config->getInt8("mqttcommproto", &mqttEnabled)) {
+    config->setMqttCommProtocolEnabled(true);
+    config->commit();
+  }
+#else
+  (void)config;
+#endif
+}
+}  // namespace
+
 ConfigOption::ConfigOption(uint8_t key, const char *value, int maxLength, bool loadKey)
     : _key(key), _value(nullptr), _maxLength(maxLength), _loadKey(loadKey) {
   if (maxLength > 0) {
@@ -679,6 +693,20 @@ uint8_t SuplaConfigManager::load(bool configParse) {
   }
 }
 
+bool SuplaConfigManager::init() {
+  const bool result = Supla::SPIFFSConfig::init();
+  if (result) {
+    enableMqttIfMissing(this);
+  }
+  return result;
+}
+
+void SuplaConfigManager::initDefaultDeviceConfig() {
+#ifdef ARDUINO_ARCH_ESP32
+  setMqttCommProtocolEnabled(true);
+#endif
+}
+
 uint8_t SuplaConfigManager::save() {
   if (SPIFFSbegin()) {
     int i = 0;
@@ -949,4 +977,4 @@ bool SuplaConfigManager::getUInt8(const char *key, uint8_t *result) {
   return KeyValue::getUInt8(key, result);
 }
 
-#pragma GCC diagnostic pop
\ No newline at end of file
+#pragma GCC diagnostic pop
diff --git a/src/SuplaConfigManager.h b/src/SuplaConfigManager.h
index 647fe2ba..5e69f309 100644
--- a/src/SuplaConfigManager.h
+++ b/src/SuplaConfigManager.h
@@ -243,6 +243,8 @@ enum _function
   FUNCTION_PZEM_RX,
   FUNCTION_PZEM_TX,
   FUNCTION_CSE7766_RX,
+  FUNCTION_CSE7761_RX,
+  FUNCTION_CSE7761_TX,
   FUNCTION_MPX_5XXX,
   FUNCTION_ANALOG_READING,
   FUNCTION_BUTTON_STOP,
@@ -315,6 +317,7 @@ class ConfigOption {
 class SuplaConfigManager : public Supla::SPIFFSConfig {
  public:
   explicit SuplaConfigManager();
+  bool init() override;
   bool SPIFFSbegin();
   bool migrationConfig();
   uint8_t addKey(uint8_t key, int maxLength, bool loadKey = true);
@@ -362,6 +365,7 @@ class SuplaConfigManager : public Supla::SPIFFSConfig {
   bool getWiFiPassword(char *result) override;
 
   bool getUInt8(const char *key, uint8_t *result) override;
+  void initDefaultDeviceConfig() override;
 
  private:
   int _optionCount;
diff --git a/src/SuplaDeviceGUI.cpp b/src/SuplaDeviceGUI.cpp
index 0e566510..e1846f17 100644
--- a/src/SuplaDeviceGUI.cpp
+++ b/src/SuplaDeviceGUI.cpp
@@ -24,6 +24,13 @@ void begin() {
   setupConnection();
   enableConnectionSSL(ConfigESP->checkSSL());
 
+#ifdef ARDUINO_ARCH_ESP32
+  static MqttClient *mqtt = nullptr;
+  if (mqtt == nullptr) {
+    mqtt = new MqttClient(&SuplaDevice);
+  }
+#endif
+
 #ifdef BUILD_VERSION
   String ver = "GG v" + String(BUILD_VERSION);
   ver.reserve(16);
@@ -816,6 +823,36 @@ void addCSE7766(int8_t pinRX) {
 }
 #endif
 
+#ifdef SUPLA_CSE7761
+Supla::Sensor::CSE_7761 *counterCSE7761 = nullptr;
+
+void addCSE7761(HardwareSerial &serial, int8_t pinRX, int8_t pinTX) {
+  if (counterCSE7761 == NULL && pinRX != OFF_GPIO && pinTX != OFF_GPIO) {
+    counterCSE7761 = new Supla::Sensor::CSE_7761(serial, pinRX, pinTX);
+
+#ifdef SUPLA_CONDITIONS
+    Supla::GUI::Conditions::addConditionsSensor(SENSOR_CSE7766, S_CSE7761, counterCSE7761);
+#endif
+  }
+  eeprom.setStateSavePeriod(TIME_SAVE_PERIOD_IMPULSE_COUNTER_SEK * 1000);
+}
+#endif
+
+#ifdef SUPLA_BL0930
+Supla::Sensor::BL_0930 *counterBL0930 = nullptr;
+
+void addBL0930(int8_t pinCF) {
+  if (counterBL0930 == NULL && pinCF != OFF_GPIO) {
+    counterBL0930 = new Supla::Sensor::BL_0930(pinCF);
+
+#ifdef SUPLA_CONDITIONS
+    Supla::GUI::Conditions::addConditionsSensor(SENSOR_HLW8012, S_BL0930, counterBL0930);
+#endif
+  }
+  eeprom.setStateSavePeriod(TIME_SAVE_PERIOD_IMPULSE_COUNTER_SEK * 1000);
+}
+#endif
+
 #ifdef SUPLA_ADE7953
 Supla::Sensor::ADE7953 *couterADE7953;
 
diff --git a/src/SuplaDeviceGUI.h b/src/SuplaDeviceGUI.h
index dc610b87..24657843 100644
--- a/src/SuplaDeviceGUI.h
+++ b/src/SuplaDeviceGUI.h
@@ -120,6 +120,12 @@
 #ifdef SUPLA_CSE7766
 #include <supla/sensor/CSE_7766.h>
 #endif
+#ifdef SUPLA_CSE7761
+#include "src/sensor/CSE_7761.h"
+#endif
+#ifdef SUPLA_BL0930
+#include "src/sensor/BL_0930.h"
+#endif
 
 #include "src/control/PinStatusLedGUI.h"
 #include "src/control/Pushover.h"
@@ -190,6 +196,10 @@
 
 #include "src/improv/improv_serial_component.h"
 #include "src/network/SuplaGuiWiFi.h"
+#include "src/network/SuplaGuiMqtt.h"
+#ifdef SUPLA_ZIGBEE_GATEWAY
+#include "src/zigbee/SuplaZigbeeGateway.h"
+#endif
 
 #ifdef SUPLA_VINDRIKTNING_IKEA_KPOP
 #include "src/sensor/VindriktningIkea.h"
@@ -366,6 +376,16 @@ extern Supla::Sensor::CSE_7766 *counterCSE7766;
 void addCSE7766(int8_t pinRX);
 #endif
 
+#ifdef SUPLA_CSE7761
+extern Supla::Sensor::CSE_7761 *counterCSE7761;
+void addCSE7761(HardwareSerial &serial, int8_t pinRX, int8_t pinTX);
+#endif
+
+#ifdef SUPLA_BL0930
+extern Supla::Sensor::BL_0930 *counterBL0930;
+void addBL0930(int8_t pinCF);
+#endif
+
 #ifdef SUPLA_ADE7953
 extern Supla::Sensor::ADE7953 *couterADE7953;
 void addADE7953(int8_t pinIRQ);
diff --git a/src/SuplaWebPageOther.cpp b/src/SuplaWebPageOther.cpp
index d1c9c22e..f820951b 100644
--- a/src/SuplaWebPageOther.cpp
+++ b/src/SuplaWebPageOther.cpp
@@ -126,7 +126,7 @@ void handleOther(int save) {
 #endif
 
 #ifdef SUPLA_CSE7766
-  addFormHeader(String(S_GPIO_SETTINGS_FOR) + S_SPACE + F("CSE7766"));
+  addFormHeader(String(S_GPIO_SETTINGS_FOR) + S_SPACE + S_CSE7766);
   addListGPIOBox(INPUT_CSE7766_RX, S_RX, FUNCTION_CSE7766_RX);
   if (ConfigESP->getGpio(FUNCTION_CSE7766_RX) != OFF_GPIO) {
     float count = Supla::GUI::counterCSE7766->getCounter();
@@ -137,6 +137,33 @@ void handleOther(int save) {
   addFormHeaderEnd();
 #endif
 
+#ifdef SUPLA_CSE7761
+  addFormHeader(String(S_GPIO_SETTINGS_FOR) + S_SPACE + S_CSE7761);
+  addListGPIOBox(INPUT_CSE7761_RX, S_RX, FUNCTION_CSE7761_RX);
+  addListGPIOBox(INPUT_CSE7761_TX, S_TX, FUNCTION_CSE7761_TX);
+  if (ConfigESP->getGpio(FUNCTION_CSE7761_RX) != OFF_GPIO && ConfigESP->getGpio(FUNCTION_CSE7761_TX) != OFF_GPIO && Supla::GUI::counterCSE7761) {
+    selected = Supla::GUI::counterCSE7761->getCurrentChannel();
+    addListBox(INPUT_CSE7761_CHANNEL, F("Kanał prądu"), CURRENT_CHANNEL_P, 2, selected);
+    float count = Supla::GUI::counterCSE7761->getCounter();
+    addNumberBox(INPUT_COUNTER_CHANGE_VALUE_CSE7761, String(S_IMPULSE_COUNTER_CHANGE_VALUE) + S_SPACE + F("[kWh]"), F("kWh"), false,
+                 String(count / 100 / 1000));
+  }
+  addFormHeaderEnd();
+#endif
+
+#ifdef SUPLA_BL0930
+  addFormHeader(String(S_GPIO_SETTINGS_FOR) + S_SPACE + S_BL0930);
+  addListGPIOBox(INPUT_BL0930_CF, F("CF"), FUNCTION_CF);
+  if (ConfigESP->getGpio(FUNCTION_CF) != OFF_GPIO && Supla::GUI::counterBL0930) {
+    float count = Supla::GUI::counterBL0930->getCounter();
+    addNumberBox(INPUT_COUNTER_CHANGE_VALUE_BL0930, String(S_IMPULSE_COUNTER_CHANGE_VALUE) + S_SPACE + F("[kWh]"), F("kWh"), false,
+                 String(count / 100 / 1000));
+    addNumberBox(INPUT_BL0930_PULSE_CONSTANT, F("Imp/kWh"), F("3200"), false, String(Supla::GUI::counterBL0930->getPulseConstant()));
+    addLabel(F("BL0930 uses CF pulses for active power and energy. Voltage/current are not exposed by this driver."));
+  }
+  addFormHeaderEnd();
+#endif
+
 #ifdef SUPLA_PZEM_V_3
   addFormHeader(String(S_GPIO_SETTINGS_FOR) + S_SPACE + F("PZEM-004T V3") + S_SPACE + S_ELECTRIC_PHASE);
   for (nr = 1; nr <= 3; nr++) {
@@ -369,6 +396,41 @@ void handleOtherSave() {
   }
 #endif
 
+#ifdef SUPLA_CSE7761
+  if (!WebServer->saveGPIO(INPUT_CSE7761_RX, FUNCTION_CSE7761_RX) || !WebServer->saveGPIO(INPUT_CSE7761_TX, FUNCTION_CSE7761_TX)) {
+    handleOther(6);
+    return;
+  }
+  else {
+    Supla::GUI::addCSE7761(
+        ConfigESP->getHardwareSerial(ConfigESP->getGpio(FUNCTION_CSE7761_RX), ConfigESP->getGpio(FUNCTION_CSE7761_TX)),
+        ConfigESP->getGpio(FUNCTION_CSE7761_RX), ConfigESP->getGpio(FUNCTION_CSE7761_TX));
+    if (strcmp(WebServer->httpServer->arg(INPUT_CSE7761_CHANNEL).c_str(), "") != 0 && Supla::GUI::counterCSE7761) {
+      Supla::GUI::counterCSE7761->setCurrentChannel(WebServer->httpServer->arg(INPUT_CSE7761_CHANNEL).toInt());
+    }
+    if (strcmp(WebServer->httpServer->arg(INPUT_COUNTER_CHANGE_VALUE_CSE7761).c_str(), "") != 0 && Supla::GUI::counterCSE7761) {
+      Supla::GUI::counterCSE7761->setCounter(WebServer->httpServer->arg(INPUT_COUNTER_CHANGE_VALUE_CSE7761).toFloat() * 100 * 1000);
+      Supla::Storage::ScheduleSave(1000);
+    }
+  }
+#endif
+
+#ifdef SUPLA_BL0930
+  if (!WebServer->saveGPIO(INPUT_BL0930_CF, FUNCTION_CF)) {
+    handleOther(6);
+    return;
+  }
+  else {
+    Supla::GUI::addBL0930(ConfigESP->getGpio(FUNCTION_CF));
+    if (strcmp(WebServer->httpServer->arg(INPUT_COUNTER_CHANGE_VALUE_BL0930).c_str(), "") != 0 && Supla::GUI::counterBL0930) {
+      Supla::GUI::counterBL0930->setCounter(WebServer->httpServer->arg(INPUT_COUNTER_CHANGE_VALUE_BL0930).toFloat() * 100 * 1000);
+    }
+    if (strcmp(WebServer->httpServer->arg(INPUT_BL0930_PULSE_CONSTANT).c_str(), "") != 0 && Supla::GUI::counterBL0930) {
+      Supla::GUI::counterBL0930->setPulseConstant(WebServer->httpServer->arg(INPUT_BL0930_PULSE_CONSTANT).toInt());
+    }
+  }
+#endif
+
 #ifdef SUPLA_PZEM_V_3
   for (nr = 1; nr <= 3; nr++) {
     if (!WebServer->saveGPIO(INPUT_PZEM_RX, FUNCTION_PZEM_RX, nr) || !WebServer->saveGPIO(INPUT_PZEM_TX, FUNCTION_PZEM_TX, nr)) {
@@ -860,4 +922,4 @@ void changePZEMAddress(uint8_t address) {
     Serial.println(pzem.getAddress(), HEX);
   }
 }
-#endif
\ No newline at end of file
+#endif
diff --git a/src/SuplaWebPageOther.h b/src/SuplaWebPageOther.h
index f19af4da..5c858595 100644
--- a/src/SuplaWebPageOther.h
+++ b/src/SuplaWebPageOther.h
@@ -24,8 +24,8 @@
 #define GUI_SENSOR_OTHER
 #endif
 
-#if defined(SUPLA_HLW8012) || defined(SUPLA_PZEM_V_3) || defined(SUPLA_PZEM_ADR) || defined(SUPLA_CSE7766) || defined(SUPLA_MODBUS_SDM) || \
-    defined(SUPLA_MODBUS_SDM_ONE_PHASE) || defined(SUPLA_MODBUS_SDM_72_V2) || defined(SUPLA_INA219)
+#if defined(SUPLA_HLW8012) || defined(SUPLA_PZEM_V_3) || defined(SUPLA_PZEM_ADR) || defined(SUPLA_CSE7766) || defined(SUPLA_CSE7761) || \
+    defined(SUPLA_BL0930) || defined(SUPLA_MODBUS_SDM) || defined(SUPLA_MODBUS_SDM_ONE_PHASE) || defined(SUPLA_MODBUS_SDM_72_V2) || defined(SUPLA_INA219)
 #define GUI_OTHER_ENERGY
 #endif
 
@@ -73,6 +73,20 @@ void handleImpulseCounterSaveSet();
 #define PATH_CSE7766                       "cse7766"
 #endif
 
+#ifdef SUPLA_CSE7761
+#define INPUT_CSE7761_RX "i61rx"
+#define INPUT_CSE7761_TX "i61tx"
+#define INPUT_CSE7761_CHANNEL "i61ch"
+
+#define INPUT_COUNTER_CHANGE_VALUE_CSE7761 "icc61"
+#endif
+
+#ifdef SUPLA_BL0930
+#define INPUT_BL0930_CF "iblcf"
+#define INPUT_BL0930_PULSE_CONSTANT "iblpc"
+#define INPUT_COUNTER_CHANGE_VALUE_BL0930 "iccbl"
+#endif
+
 #if defined(SUPLA_HLW8012) || defined(SUPLA_CSE7766)
 #define PATH_CALIBRATE      "calibrate"
 #define INPUT_CALIB_POWER   "power"
diff --git a/src/src/language/common.h b/src/src/language/common.h
index 37dd9225..e44d1fa2 100644
--- a/src/src/language/common.h
+++ b/src/src/language/common.h
@@ -44,6 +44,8 @@
 #define S_BH1750                         "BH1750"
 #define S_MS5611                         "MS5611"
 #define S_CSE7766                        "CSE7766"
+#define S_CSE7761                        "CSE7761"
+#define S_BL0930                         "BL0930"
 #define S_ADE7953                        "ADE7953"
 #define S_MAX44009                       "MAX44009"
 #define S_GPIO                           "GPIO"
diff --git a/src/src/update/SuplaHTTPUpdateServer.h b/src/src/update/SuplaHTTPUpdateServer.h
index 383a2ecf..9e1f38e4 100644
--- a/src/src/update/SuplaHTTPUpdateServer.h
+++ b/src/src/update/SuplaHTTPUpdateServer.h
@@ -11,7 +11,9 @@
 #define INPUT_UPDATE_URL          "iuu"
 #define PATH_UPDATE               "updateOTA"
 
+#ifndef HOST_BUILDER
 #define HOST_BUILDER "http://gui-generic-builder.supla.io/"
+#endif
 
 class HTTPUpdateServer {
  public:
@@ -40,4 +42,4 @@ class HTTPUpdateServer {
   void updateManual();
 };
 #endif
-#endif
\ No newline at end of file
+#endif
